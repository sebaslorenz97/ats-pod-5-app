import { Routes } from '@angular/router';

import { MainComponent } from './domains/home/main/main.component'

export const routes: Routes = [
  {
    path: 'home',
    component: MainComponent
  }
];
